import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF4A90E2);
  static const Color primaryIndigo = Color(0xFF6A5AE0);
  static const Color accentTeal = Color(0xFF3EDBF0);
  static const Color dark = Color(0xFF1E1E2F);
  static const Color backgroundLight = Color(0xFFF5F7FB);
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF1E1E2F);
  static const Color textSecondary = Color(0xFF6B6F80);

  static const List<Color> classColors = [
    Color(0xFF2E7D32), // Cultivator - Green
    Color(0xFF8D6E63), // Harrow - Brown
    Color(0xFF795548), // Plough - Dark Brown
    Color(0xFF6D4C41), // Rake - Light Brown
    Color(0xFFF57C00), // Rotivator - Orange
    Color(0xFF1976D2), // Seeder - Blue
    Color(0xFF424242), // Truck - Gray
    Color(0xFF0288D1), // Sprinkler - Light Blue
    Color(0xFFD32F2F), // Harvester - Red
    Color(0xFF388E3C), // Tractor - Green
  ];

  static const List<String> classNames = [
    'Farm Cultivator',
    'Farm Harrow',
    'Farm Plough',
    'Farm Rake',
    'Farm Rotivator',
    'Farm Seeder',
    'Farm Truck',
    'Farm Sprinkler',
    'Farm Harvester',
    'Farm Tractor',
  ];
}
